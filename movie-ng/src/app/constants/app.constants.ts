const server_url = 'http://localhost:3000';
export const APP_CONSTANTS = {
    SERVER_URL: server_url,
    movies: server_url + '/movies',
    search: server_url + '/search',
    movieBy: server_url + '/movieBy'
}